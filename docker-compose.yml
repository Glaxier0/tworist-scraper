version: '3.4'

services:
  MongoDb:
    image: mongo:latest
    container_name: tworist_mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin
      MONGO_INITDB_DATABASE: tworist
    volumes:
      - ./init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js:ro
      - ./mongo-volume:/data/db
#    networks:
#      new:
#        aliases:
#          - mongodb
#  Redis:
#      image: redis:latest
#      container_name: recommaster_redis
#      networks:
#        new:
#          aliases:
#            - redis
#      ports:
#          - "6379:6379"
#networks:
#  new:
volumes:
#  diagnostics:
  mongodb:
#  data:
#  esdata:
#    driver: local
